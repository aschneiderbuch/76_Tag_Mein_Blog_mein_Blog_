import fs from 'fs'


// **** loadFile
export const loadFile = () => {
        // Promise für fetch
        return new Promise ( (resolve, reject ) => {
            // liest Datei ./dpPosts.json
            
        })
}




// **** saveFile
export const saveFile = () => {

}